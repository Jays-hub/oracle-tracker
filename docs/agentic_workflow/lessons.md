# Agentic Workflow — lessons

Dated log of mistakes made while operating this workflow, each paired with a concise guideline to
prevent recurrence. Newest entries appended at the bottom. Scope + access rule: `README.md`.

(No entries yet — add one the first time the loop misfires, with the guideline that prevents a repeat.)
